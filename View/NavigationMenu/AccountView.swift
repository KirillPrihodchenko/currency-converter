import SwiftUI

struct AccountView: View {
    var body: some View {
        ZStack {
                
            Text("Account")
        }
        .background(Color(#colorLiteral(red: 0.2232421875, green: 0.2232421875, blue: 0.2232421875, alpha: 1)))
    }
}
